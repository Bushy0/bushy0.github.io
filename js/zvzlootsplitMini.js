const STARTING_GUILDS = 2; let guildCount = 0; const guildInputs = document.getElementById("guildInputs"), splitValueInput = document.getElementById("splitValue"), results = document.getElementById("splitResults"), addGuildButton = document.getElementById("addGuild"); function addGuild() { guildCount++; const n = document.createElement("div"); n.className = "input-container", n.dataset.guildId = guildCount, n.innerHTML = `\n\n\n                <div class="field">\n\n\n                    <label\n                        for="guild${guildCount}Name"\n                    >\n                        Guild Name\n                    </label>\n\n\n                    <input\n                        type="text"\n                        id="guild${guildCount}Name"\n                        value="Guild ${guildCount}"\n                        autocomplete="off"\n                    >\n\n\n                </div>\n\n\n                <div class="field">\n\n\n                    <label\n                        for="guild${guildCount}Attendance"\n                    >\n                        Attendance\n                    </label>\n\n\n                    <input\n                        type="number"\n                        id="guild${guildCount}Attendance"\n                        min="0"\n                        value="0"\n                        inputmode="numeric"\n                    >\n\n\n                </div>\n\n\n                <button\n                    type="button"\n                    class="remove-guild"\n                    title="Remove Guild"\n                    aria-label="Remove Guild"\n                >\n\n\n                    <i class="bx bx-trash"></i>\n\n\n                </button>\n\n\n            `, guildInputs.appendChild(n); n.querySelector(".remove-guild").addEventListener("click", () => { n.remove(), calculateSplits() }) } for (let n = 0; n < 2; n++)addGuild(); function calculateSplits() { const n = Number(splitValueInput.value.replace(/,/g, "")) || 0, t = []; guildInputs.querySelectorAll(".input-container").forEach(n => { const e = n.querySelector('input[type="text"]'), l = n.querySelector('input[type="number"]'); if (!e || !l) return; const u = e.value.trim() || `Guild ${n.dataset.guildId}`, a = Number(l.value) || 0; t.push({ name: u, attendance: a }) }); const e = t.filter(n => n.attendance > 0), l = e.reduce((n, t) => n + t.attendance, 0); if (n <= 0 || l <= 0) return results.innerHTML = '\n\n\n                    <div class="results-header">\n\n\n                        <h2>\n                            Split Results\n                        </h2>\n\n\n                        <button\n                            type="button"\n                            id="copyResultsButton"\n                        >\n\n\n                            <i class="bx bx-copy"></i>\n\n\n                            Copy Results\n\n\n                        </button>\n\n\n                    </div>\n\n\n                    <p>\n                        Enter a loot value and at least\n                        one attendance value.\n                    </p>\n\n\n                ', void setupCopyButton(null); let u = 0; const a = e.map(t => { const e = Math.floor(t.attendance / l * n); return u += e, { name: t.name, attendance: t.attendance, share: e } }), i = n - u; a.length > 0 && (a[a.length - 1].share += i); const s = a.map(n => `\n\n\n                            <p class="result">\n\n\n                                <span class="result-name">\n\n\n                                    ${escapeHtml(n.name)}\n\n\n                                </span>\n\n\n                                <span class="result-value">\n\n\n                                    ${n.share.toLocaleString()}\n\n\n                                </span>\n\n\n                            </p>\n\n\n                        `).join(""), o = a.reduce((n, t) => n + t.share, 0); results.innerHTML = `\n\n\n                <div class="results-header">\n\n\n                    <h2>\n                        Split Results\n                    </h2>\n\n\n                    <button\n                        type="button"\n                        id="copyResultsButton"\n                    >\n\n\n                        <i class="bx bx-copy"></i>\n\n\n                        Copy Results\n\n\n                    </button>\n\n\n                </div>\n\n\n                ${s}\n\n\n            `, setupCopyButton({ splitValue: n, shares: a, totalDistributed: o }) } function setupCopyButton(n) { const t = document.getElementById("copyResultsButton"); t && t.addEventListener("click", async () => { if (!n) return t.innerHTML = '\n\n\n                            <i class="bx bx-x"></i>\n\n\n                            Nothing to Copy\n\n\n                        ', void setTimeout(() => { t.innerHTML = '\n\n\n                                    <i class="bx bx-copy"></i>\n\n\n                                    Copy Results\n\n\n                                ' }, 1500); let e = "ZvZ Loot Split\n"; e += `Total Loot: ${n.splitValue.toLocaleString()}\n`, e += "-------------------------\n", n.shares.forEach(n => { e += `${n.name}: ${n.share.toLocaleString()}\n` }), e += "-------------------------\n"; try { await navigator.clipboard.writeText(e), t.innerHTML = '\n\n\n                            <i class="bx bx-check"></i>\n\n\n                            Copied!\n\n\n                        ', setTimeout(() => { t.innerHTML = '\n\n\n                                    <i class="bx bx-copy"></i>\n\n\n                                    Copy Results\n\n\n                                ' }, 1500) } catch (n) { console.error("Copy failed:", n), t.innerHTML = '\n\n\n                            <i class="bx bx-x"></i>\n\n\n                            Copy Failed\n\n\n                        ', setTimeout(() => { t.innerHTML = '\n\n\n                                    <i class="bx bx-copy"></i>\n\n\n                                    Copy Results\n\n\n                                ' }, 1500) } }) } function escapeHtml(n) { const t = document.createElement("div"); return t.textContent = n, t.innerHTML } document.addEventListener("click", n => { n.target.matches("input") && n.target.select() }), document.addEventListener("input", n => { n.target.matches("input") && calculateSplits() }), splitValueInput.addEventListener("blur", () => { const n = splitValueInput.value.replace(/\D/g, ""); splitValueInput.value = n ? Number(n).toLocaleString() : "0", calculateSplits() }), addGuildButton.addEventListener("click", () => { addGuild(), calculateSplits() }), calculateSplits();